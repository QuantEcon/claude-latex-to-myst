__pycache__/
*.pyc
.venv/
_build/
tmp/
.DS_Store

# uv.lock IS committed (reproducible installs); only the venv is ignored.

# Local copies of sibling book repos used for parity testing. Populated by
# scripts/setup_fixtures.sh; never committed (they would be ~megabytes of
# duplicated text whose canonical home is the upstream book-dp1 / book-dp2
# repos). See reports/README.md for the verification workflow.
fixtures/
.coverage
QUALITY-REVIEW.md

# Transitional session prompts / handover briefings stay local (user pref):
# work-plan artifacts, not durable architecture docs.
notes/SESSION-PROMPT-*.md
