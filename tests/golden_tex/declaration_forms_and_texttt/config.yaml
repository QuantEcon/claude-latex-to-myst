# #107 gap1 + #105: {\sc ..} declaration forms keep their formatting via
# \textsc{..}, and \texttt{{@}foo} flattens the {@} grouping (pandoc would
# otherwise emit two adjacent code spans).
source_dir: "."
output_dir: "."
tmp_dir: "./tmp"

chapters:
  - { stem: input, title: "Declaration Forms" }
