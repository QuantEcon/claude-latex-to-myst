\chapter*{Preface}

An unlabelled chapter: pandoc derives ``preface'' for the H1. Depth 1 is
gated out of the suppression because \texttt{add\_frontmatter} keys on the
anchor to build the page \texttt{label:}.

\section{Value Iteration}
\label{sec:vi}

An author-labelled section. The slug is \texttt{sec:vi}, which pandoc could
never derive from the title, so the anchor is promoted.

\section*{Convergence}
\label{sec:conv}

A starred section that the author still labelled. Starred forces an
attribute block, but the id inside it is the author's, so it is promoted.

\section*{Background reading}

A starred, unlabelled section. Pandoc mints ``background-reading'' from the
title and only emits it because \texttt{.unnumbered} forces the attribute
block. Nothing here is a cross-reference target, so no anchor.

\section*{Exercises}

Repeat this title in a second chapter and the derived anchors collide
project-wide --- the reported symptom.

\section*{Exercises}

Pandoc's within-file dedup suffix. Unique by construction, so it never
collides; suppressing it would not be identifier-neutral, so it is kept.

Refer back to \Cref{sec:vi} and \Cref{sec:conv} to confirm both author
labels still resolve.
