# Seeded from lesson 043: figure caption citation recovered (no key drop / bracket leak)
source_dir: "."
output_dir: "."
tmp_dir: "./tmp"

chapters:
  - { stem: input, title: "Figure Caption Citation" }
