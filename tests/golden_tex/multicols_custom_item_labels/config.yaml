# #111 items 11/13: a multicols-wrapped enumerate whose every \item carries
# an explicit [label] (dp1's norm-properties shape). The multicols wrapper
# unwraps without leaking the column count; the custom (a)-(d) labels
# survive as labelled paragraphs instead of pandoc's renumbered 1..N list.
source_dir: "."
output_dir: "."
tmp_dir: "./tmp"

chapters:
  - { stem: input, title: "Custom Item Labels" }
