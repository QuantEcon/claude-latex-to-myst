# The consumer pre-renders the inline tikzpicture as an SVG and maps it by
# the figure's label. caption_override=None ⇒ the source caption (math
# intact) is used.
TIKZ_FIGURE_MAP = {
    'fig-reg': ('figures/reg.svg', None),
}
